name: Public source safety

on:
  push:
  pull_request:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Audit tracked files and Git history
        run: python3 tools/audit-public-source.py
      - name: Verify Minimal OpenCV r1 usage boundary
        run: python3 tools/verify-opencv-min-r1-usage.py app/src
      - name: Audit responsive page-width invariants
        run: python3 tools/audit-responsive-ui.py
      - name: Audit Security r2 source boundaries
        run: python3 tools/audit-security-r2-source.py
