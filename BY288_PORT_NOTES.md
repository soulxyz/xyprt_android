# X1 / BY-288 适配说明

## 蓝牙兼容

目前确认至少存在两种通信形态：

- Bluetooth Classic RFCOMM / SPP：UUID `00001101-0000-1000-8000-00805F9B34FB`
- BLE GATT：Service `FF00`，写入 `FF02`，主通知 `FF01`，可选旁路通知 `FF03`

应用会自动发现并选择通道。双模设备优先沿用已经实测稳定的 SPP；只有 BLE 的设备会透明使用 BLE，用户不需要手动选择。

BLE 兼容实现按 V1.05 实机行为保守处理：

- FF03 与 FF01 分开接收，避免连接时的旁路上报污染查询响应
- 优先使用带响应写入
- Android 端不主动调用 `requestMtu()`，默认从 **20 字节**分包开始
- 如果 FF02 返回 GATT 13（长度不兼容），自动按 `20 → 16 → 12 → 8` 降低分包并重试同一段数据
- BLE fallback 可发起系统 Bond；新 Bond 后会短暂寻找 Classic/SPP，双模设备仍优先沿用 SPP
- FF02 不存在时可尝试 ISSC 写入特征作为兼容兜底

这一策略用于兼容 `Qring_5DEA / V1.05 / v3.38.21_AY` 一类仅 BLE 或 BLE 可用的设备，同时不改变原有 SPP 用户的默认路径。

## 打印协议

- 打印数据：GS v 0 raster，384 点纸宽，按连续纸走纸方向输出
- 打印宽度由设备硬件决定，UI 不要求用户填写纸宽
- 打印前后走纸可在打印页小范围调整并记住
- `10 FF 70` 不用于正常状态查询；设备信息分别使用型号、固件、序列号等独立查询命令

## 暂不提供

- 退纸命令目前没有经过 BY-288 真机验证成功，因此正式界面不提供假退纸功能
- 固件更新功能保持禁用，避免在没有可靠原厂固件备份时误擦写设备
